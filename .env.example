# --- Shared ---
REDIS_URL=redis://localhost:6379

# --- inventory-service ---
INVENTORY_PORT=4001
TICKET_COUNT=500
STREAM_KEY=ticket_reservations
ADMIN_RESET_SECRET=change-me-in-production

# --- order-service ---
# WebSocket is served on this same port, path /ws — see src/server.js
ORDER_PORT=4002
DATABASE_URL=./order-service/data/orders.db
CONSUMER_GROUP=order-service-group
CONSUMER_NAME=order-service-1
